# Fetch git dependencies with the system `git` binary so Cargo uses the same
# credential helper, proxy, and certificate settings as the developer's shell.
# The selene-db substrate is a public git dependency pinned in Cargo.lock; no
# secrets live here.
[net]
git-fetch-with-cli = true
